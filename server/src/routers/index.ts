import authRouter from './auth';
import strainRouter from './strain';
import growLogRouter from './growLog';
import logEntryRouter from './logEntry';
import userRouter from './users';

const routers = {
  authRouter,
  strainRouter,
  growLogRouter,
  logEntryRouter,
  userRouter,
};

export default routers;
